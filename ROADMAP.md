# Roadmap

This is the current improvement plan for Frontend No-Slop. Keep historical audits in `IMPROVEMENT_REVIEW.md`; keep active work here.

## Next

- Add live agent eval runs that compare output with and without the skill.
- Add example outputs for `form`, `modal-dialog`, `design-system`, and `app-shell`.
- Add a compatibility note for native skill-directory installs in clients that prefer `.claude/skills/` or similar layouts.
- Add release tags and GitHub release notes that match `metadata.version`.

## Later

- Add optional helper scripts for packaging a release archive.
- Add a docs page section for eval coverage and schema contracts.
- Add stricter schema annotations for component state matrices.
- Add a small fixture set for visual audit prompts with screenshot-region evidence.

## Done

- CI branch coverage fixed for the current default branch.
- Cross-platform validation path documented.
- Adapter templates separated from repository maintainer instructions.
- Page-type registry and schema alignment added.
- Static eval manifest added.
- Accessibility and performance guidance tied to public standards.
